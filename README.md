# RouteObjects Homebrew Tap

Homebrew formulae for RouteObjects command-line tools.

## Install

Prebuilt binaries for macOS and Linux. No Swift toolchain is required at
install time.

```bash
brew install RouteObjects/tap/cidrwalk
```
